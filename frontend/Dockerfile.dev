FROM node:20-alpine

WORKDIR /app

# Expose the Next.js port
EXPOSE 3000

# Start Next.js in development mode with hot reload
# Dependencies are provided via volume mount from host
CMD ["npm", "run", "dev"]
